
# -- Arquivos de Ambiente --
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# -- Dependências --
# A pasta que contém todos os pacotes baixados.
node_modules

# -- Logs --
# Arquivos de log de erro do NPM, PNPM e Yarn.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# -- TypeScript (Arquivos de Build) --
# Pasta de saída do TypeScript (o JavaScript compilado).
dist
build
.tsbuildinfo

# Se você compilar TS para JS na mesma pasta (o que não é recomendado, mas por via das dúvidas)
*.js
*.js.map
*.mjs
*.cjs

# -- Sistema Operacional --
# Arquivos do macOS
.DS_Store

# Arquivos do Windows
Thumbs.db
ehthumbs.db

# -- Editores de Código --
# VSCode
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json

# JetBrains (WebStorm, etc.)
.idea/

# -- Testes --
# Pasta de cobertura de testes gerada pelo Vitest/Jest
coverage/
.nyc_output/

# -- Outros --
# Arquivos de lock específicos de instalação (pnpm)
.pnpm-store/